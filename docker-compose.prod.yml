services:
  postgres:
    restart: unless-stopped
  n8n:
    restart: unless-stopped
  caddy:
    image: caddy:2
    restart: unless-stopped
    ports: ["80:80", "443:443"]
    volumes:
      - ./infra/caddy/Caddyfile.prod.example:/etc/caddy/Caddyfile:ro
