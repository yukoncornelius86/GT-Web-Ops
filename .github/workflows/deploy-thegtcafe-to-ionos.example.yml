name: Deploy GT Cafe Example
on:
  push:
    branches: [main]
    paths: ["sites/thegtcafe/**"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Use IONOS_SFTP_* secrets"
